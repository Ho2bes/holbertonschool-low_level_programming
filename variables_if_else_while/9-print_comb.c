#include <stdio.h>

/**
*main - Entry point
*
*Return: always 0 (Success)
*/

int main(void)
{
char numero;
char virgule = ","
char espace = " "
for (numero = '0'; numero <= '9'; numero++)
putchar(numero, virgule, espace);
putchar('\n');
return (0);
}
