#include "main.h"

    /**
    *print_alphabet - Entry point
    *
    *Return: always 0 (Success)
    */

void times_table(void)

{
	{
	int x;
	int y;
	
	for (x = 0; x <=9; x++)
		
	
		for (y = 0; y <=9; y++)
			_putchar ((x * y) + '0');
			_putchar (',');
			_putchar (' ');
			_putchar (' ');
	}
			if ((x * 9) && (y * 9))
			{
				_putchar ('\n');
				else
					return(void);
	}
}
