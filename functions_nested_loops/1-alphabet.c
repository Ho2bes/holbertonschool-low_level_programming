#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
*main - Entry point
*
*Return: always 0 (Success)
*/

int main(void)
{
char alpha = 'a';
while (alpha <= 'z')
_putchar(alpha);
alpha++
_putchar('\n');
return (0);
}
