#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
*main - Entry point
*
*Return: always 0 (Success)
*/

int main(void)
{
char ch
for (ch = 'a'; ch <= 'z'; ch++)
_putchar(ch);
_putchar('\n');
return (0);
}
