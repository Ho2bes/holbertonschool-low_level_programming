#include "main.h"

/**
*_islower - Entry point
*
*Return: always 0 (Success)
*/

int _islower(int c)
{
int i;
i = 0;
if(i >= 61)
{
 _putchar('1', i, _islower(i));
 }
Else
{
 _putchar('0', i, _islower (i));
}
return (0);
}
