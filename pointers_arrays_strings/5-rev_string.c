#include "main.h"
#include "2-strlen.c"

/**
 *rev_string - reverse a string
 *
 *Return: void
 */

void rev_string(char *s)
{

while (*s != '\0')
	{
_putchar(*str);
str++;
}
_putchar('\n');
}
void rev_string(char *s)
	{
int i;
for (i = _strlen(s) - 1; i >= '\0'; i--)
{
_putchar(s[i]);
}
